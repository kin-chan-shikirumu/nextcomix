<h2>2021～2025年の発行部数ランキング</h2><ol>
  <% @ranking_tweets.each do |tweet| %>
    <li>
      <%= link_to tweet.title, tweet_path(tweet) %> - <%= tweet.circulationtotwentytwentyfive %> 部
    </li>
  <% end %>
</ol>